import React from "react";

const Home = ({ color }) => {
  return (
    <article style={{ backgroundColor: color }}>
      <h2>Welcome to the homepage...</h2>
    </article>
  );
};

export default Home;
